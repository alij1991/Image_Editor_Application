# Phase XVI.65 — minimum deps for the ONNX-export scripts.
#
# Pinned loosely so the user's existing PyTorch install can satisfy
# them. Any modern version (PyTorch >= 1.13) handles `torch.onnx.export`
# the way these scripts use it.

torch>=2.0
torchvision>=0.15
onnx>=1.14
onnxruntime>=1.16
huggingface_hub>=0.20
numpy>=1.24

# convert_harmonizer.py: the upstream ZHKKKe/Harmonizer source
# imports `kornia` at the top of `src/model/filter.py`, so kornia
# has to be importable when we trace the model. The upstream
# `src/requirements.txt` doesn't pin a version; any modern release
# is fine.
kornia>=0.7

# convert_photo_wct2.py: the upstream chiutaiyin/PhotoWCT2 demo
# uses Pillow for image I/O; pulled in defensively so users can
# also run the upstream `transfer.py` if they want to validate
# their checkpoint outside of ONNX export.
Pillow>=10.0
